<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ictproject.moviemate.domain.review.mapper.ReviewMapper">

    <insert id="insertReview">
        INSERT INTO review
            (user_id, movie_cd, text, sympathy, review_date, grade)
        VALUES
            (#{userId}, #{movieCd}, #{text}, #{sympathy}, #{reviewDate}, #{grade})
    </insert>

    <update id="modifyReview">
        UPDATE
            review
        SET
            text = #{text},
            review_date = CURRENT_TIMESTAMP
            grade = #{grade}
        WHERE
            review_id = #{reviewId}
    </update>

    <delete id="deleteReview">
        DELETE FROM
            review
        WHERE
            review_id = #{reviewId}
    </delete>

    <select id="findReview" resultType="review">
        SELECT
            *
        FROM
            review
        WHERE
            review_id = #{reviewId}
    </select>

    <select id="findReviewsByMovie" resultType="review">
        SELECT
            *
        FROM
            review
        WHERE
            movie_cd = #{mc}
        ORDER BY
            review_id
        LIMIT #{p.pageStart}, #{p.amount}
    </select>

    <select id="findReviewsByUser" resultType="review">
        SELECT
            *
        FROM
            review
        WHERE
            user_id = #{ui}
        ORDER BY
            review_id
        LIMIT #{p.pageStart}, #{p.amount}
    </select>

    <select id="countByMovie">
        SELECT
            COUNT(*)
        FROM
            review
        WHERE
            movie_cd = #{movieCd}
    </select>

    <select id="countByUser">
        SELECT
            COUNT(*)
        FROM
            review
        WHERE
            user_cd = #{userId}
    </select>

</mapper>